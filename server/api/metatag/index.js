'use strict';

var express = require('express');
var controller = require('./metatag.controller');
var auth = require('../../auth/auth.service');
var router = express.Router();

// router.get('/', controller.get);

module.exports = router;
