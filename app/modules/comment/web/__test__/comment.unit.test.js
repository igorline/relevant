// import { act } from 'react-dom/test-utils';
// import 'jest-styled-components';
// import { CommentFormComponent } from 'modules/comment/web/commentForm.component';
// import { post, user } from 'mockdata';
// import { withEnzyme } from 'jest.setup';

// const state = {
//   auth: { user: user.user1, isAuthenticated: true },
//   navigation: { screenSize: 0 }
// };

// const props = {
//   nestingLevel: 0,
//   buttonText: 'Comment',
//   parentPost: post.linkPost1,
//   comment: post.post1,
//   additionalNesting: 0,
//   history: {},
//   edit: true
// };

describe('components', () => {
  describe('CommentForm', () => {
    it('should render self and subcomponents', () => {
      // const wrapper = withEnzyme({ state, props, Component: CommentForm });
      // const textarea = wrapper.find('textarea');
      // const cancelButton = wrapper.find('button').filterWhere(n => n.text() === 'Cancel');
      // console.log(textarea.debug());
      // // cancelButton.simulate('mousedown');
      // // act(() => cancelButton.prop('onMouseDown')());
      // act(() => textarea.prop('onChange')({ target: { value: 'test input' } }));
      // // textarea.prop('onChange')({ target: { value: 'test input' } });
      // wrapper.update();
      // expect(textarea.prop('value')).toBe('test input');
      // console.log(wrapper.html());
      // expect(wrapper.find('textarea').props.value()).toBe('');
      // expect(enzymeWrapper
      // .find(HandleText.displayName).text()).toBe(`@${user.handle} ${postTime}`);
    });
  });
});
